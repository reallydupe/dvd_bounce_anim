**DVD Logo bouncing animation, py**

**Also please read this: please replace the dvd logo with a proper normal one cuz the one i provided is a little bit strange when recoloring applies**

This was made in python without any AI
Feel free to mod this to add things you want

What this was made on and what libraries used:
Tkinter (The window and responsible for all the window logic), built-in library
pywinstyles (just a hefty thing that lets you apply windows themes, but here its used to  change the window's titlebar color to black), to install use: <pre><code>pip install pywinstyles</code></pre>
random (Used to randomly pick colors), built-in library

PIL (pillow), - Used to replace the square with an actual dvd logo and change its color, to install use: <pre><code>pip install pillow</code></pre>

What libs you could use in the future:
uhh idk its up to u

Also the requirements:
the ones before this or PIL and pywinstyles, how to install is also there
and pyinstaller for compiling thru the compile.bat

Yea, so this is the end of the Readme MD file. Probably the first time properly writing an .MD file
Bye! 👋
